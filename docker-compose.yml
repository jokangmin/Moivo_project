version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: ./src/main/java/Dockerfile
    ports:
      - "8080:8080"
    restart: always
  frontend:
    build:
      context: ./src/main/moivo_react
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: always
